<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Loging"
        tools:targetApi="31">

        <!-- Pantallas ya existentes -->
        <activity android:name=".Menu" android:exported="false" />
        <activity android:name=".Biblioteca" android:exported="false" />
        <activity android:name=".Registro" android:exported="false" />
        <activity android:name=".OptimizacionCorte" android:exported="false" />
        <activity android:name=".GestionTela" android:exported="false" />
        <activity android:name=".Organizacion" android:exported="false" />

        <!-- NUEVA ACTIVIDAD PRINCIPAL -->
        <activity android:name=".MainActivity" android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Actividad secundaria: Detalle del vestido -->
        <activity android:name=".VestidoDetalleActivity" android:exported="false" />

        <!-- Actividades nuevas: Calorimetría y Visagismo -->
        <activity android:name=".colorimetriaActivity" android:exported="false" />
        <activity android:name=".VisagismoActivity" android:exported="false" />
        <activity android:name=".Usuario" android:exported="false" />

    </application>

</manifest>